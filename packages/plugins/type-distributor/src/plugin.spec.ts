import { describe, it, expect } from 'vitest';

describe('Type Distributor Plugin', () => {
    it('should return a greeting message', () => {
        const message = 'Hello, Type Distributor!';
        expect(message).toBe('Hello, Type Distributor!');
    });
});